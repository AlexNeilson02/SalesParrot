import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev overlay badge sits in the bottom-left corner, on top of the
  // Map tab in the app's bottom nav.
  devIndicators: false,
};

export default nextConfig;
